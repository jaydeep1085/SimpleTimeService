# SimpleTimeService
Simple microservice (which we will call "SimpleTimeService")  The application should be a web server that returns a pure JSON response with the following structure, when its / URL path is accessed
